server:
  port: 8762

spring:
  application:
    name: user-service

eureka:
  client:
    service-url:
      defaultZone: http://admin:admin@localhost:8761/eureka/

feign:
  hystrix:
    enabled: true

xinruke:
  commodity:
    enabled: true
    config: a,b,c,1,2,3
